# Design Tokens — UI standards

> [DRAFT] Fill in colors, typography, spacing. Point to `tailwind.config.ts`, `globals.css`, or `components/ui/`.

## Source of truth

| Token type | Location |
|---|---|
| CSS variables | `[e.g. app/globals.css]` |
| Tailwind theme | `[e.g. tailwind.config.ts]` |

## Rules

- Do not hardcode hex in components — use theme tokens
- Shared primitives: `components/ui/`

Enforced by: `feedback/sensors/standards-compliance.sensor.md`
